name: ParleyMap nightly refresh

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Safety hold
        run: |
          echo "Nightly refresh is disabled."
          echo "This workflow does not modify index.html."
          echo "Use data-diagnostics.yml and data-validate.yml first."
