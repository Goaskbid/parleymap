# Recovery Summary — ParleyMap v4.0.0

Current state: v4.0.0 adds the below-map intelligence layer while preserving the accepted v3.9 map/profile layout.

Core UI: map first, profile right rail, intelligence below map.

New UI: build alerts, topic radar, recurring attendees, organization profiles, event overlap matrix, organization penetration index, topic migration, influence timeline and power geography.

Critical deployment files: `index.html`, `CNAME`, `.nojekyll`, `impressum.html`, `privacy.html`, `corrections.html`, `data/operator.json`, plus the supporting project files when using the full GitHub zip.

Legal-only replacement files: `index.html`, `impressum.html`, `privacy.html`, `corrections.html`, `data/operator.json`.
