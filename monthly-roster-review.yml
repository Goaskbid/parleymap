name: ParleyMap monthly roster review

on:
  workflow_dispatch:
  schedule:
    - cron: "31 4 1 * *"

permissions:
  contents: write

concurrency:
  group: parleymap-monthly-roster-review
  cancel-in-progress: false

jobs:
  monthly-roster-review:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Checkout full repository history
        uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Canonical repair before roster review
        env:
          REQUIRE_ADSENSE_READY: "false"
        run: node scripts/parleymap-canonical-rescue.cjs

      - name: Safe roster review diagnostics
        run: node scripts/parleymap-safe-roster-review.cjs

      - name: Show summary
        if: always()
        run: |
          if [ -f data/diagnostics/LATEST_RUN_SUMMARY.md ]; then
            cat data/diagnostics/LATEST_RUN_SUMMARY.md >> "$GITHUB_STEP_SUMMARY"
            cat data/diagnostics/LATEST_RUN_SUMMARY.md
          fi

      - name: Upload roster review files
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: parleymap-monthly-roster-review-files
          path: |
            index.html
            ads.txt
            data/demo.json
            data/diagnostics/*

      - name: Commit monthly roster review
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: "Run ParleyMap monthly safe roster review"
          file_pattern: "index.html ads.txt data/demo.json data/diagnostics/* privacy.html impressum.html about.html contact.html methodology.html data-sources.html"
