import fs from 'node:fs';
import path from 'node:path';
const root = process.cwd();
const templatePath = path.join(root,'templates','index.template.html');
const cssPath = path.join(root,'src','styles.css');
const pkgPath = path.join(root,'package.json');
let tpl = fs.readFileSync(templatePath,'utf8');
tpl = tpl.replace(/ParleyMap v3\.9/g, 'ParleyMap v4.0');
tpl = tpl.replace(/ParleyMap v3\.9\.0/g, 'ParleyMap v4.0.0');
tpl = tpl.replace('public appearances, meetings, calls and summits mapped for context with polished roadmovie playback, synchronized calls, animated contrails, heavy-hitter deep search and legal identity details.', 'public appearances, meetings, calls and summits mapped for context with alerts, agenda themes, organization profiles, overlap matrices, topic migration and power geography.');
const oldBlock = `          <article class="intel-panel wide"><h3>Influence-event catalog</h3><div id="rank-event-catalog" class="agenda-list"></div></article>\n          <article class="intel-panel wide"><h3>High-frequency connectors</h3><div id="rank-connectors" class="rank-list"></div></article>`;
const newBlock = `          <article class="intel-panel wide"><h3>Alert center</h3><div id="rank-alerts" class="agenda-list"></div></article>\n          <article class="intel-panel wide"><h3>Topics and themes</h3><div id="rank-topics" class="topic-radar"></div></article>\n          <article class="intel-panel wide"><h3>Recurring attendees</h3><div id="rank-recurring" class="rank-list"></div></article>\n          <article class="intel-panel wide"><h3>Organization profiles</h3><div id="org-profiles" class="org-list"></div></article>\n          <article class="intel-panel wide"><h3>Event overlap matrix</h3><div id="overlap-matrix" class="overlap-wrap"></div></article>\n          <article class="intel-panel wide"><h3>Organization penetration index</h3><div id="org-penetration" class="penetration-list"></div></article>\n          <article class="intel-panel wide"><h3>Topic migration</h3><div id="topic-migration" class="migration-list"></div></article>\n          <article class="intel-panel wide"><h3>Influence timeline</h3><div id="influence-timeline" class="influence-timeline"></div></article>\n          <article class="intel-panel wide"><h3>Power geography</h3><div id="power-geography" class="geo-list"></div></article>\n          <article class="intel-panel wide"><h3>Influence-event catalog</h3><div id="rank-event-catalog" class="agenda-list"></div></article>\n          <article class="intel-panel wide"><h3>High-frequency connectors</h3><div id="rank-connectors" class="rank-list"></div></article>`;
if (!tpl.includes('id="rank-alerts"')) tpl = tpl.replace(oldBlock, newBlock);
tpl = tpl.replace('ParleyMap v3.9.0 · Public appearances, meetings, calls and summits, mapped for context. Not live tracking.', 'ParleyMap v4.0.0 · Alerts, agendas, themes, organizations, overlap, geography and public roadmovies. Not live tracking.');
fs.writeFileSync(templatePath, tpl);
let css = fs.readFileSync(cssPath,'utf8');
if (!css.includes('v4.0 intelligence panels')) {
  css += `\n\n/* v4.0 intelligence panels: alerts, topics, organizations, overlap and geography. */\n.intel-grid { grid-template-columns: repeat(2, minmax(0, 1fr)); }\n.alert-row .agenda-date { min-width: 112px; color: var(--gold); }\n.topic-radar, .org-list, .penetration-list, .migration-list, .geo-list { display: grid; gap: 7px; }\n.topic-row, .org-row, .penetration-row, .migration-row, .geo-row, .month-cell { border: 1px solid var(--line); background: #111925; color: var(--text); padding: 9px; min-height: 48px; text-align: left; }\n.topic-row { display: grid; grid-template-columns: minmax(88px, 1fr) 1.6fr auto; gap: 10px; align-items: center; }\n.topic-bar { height: 10px; background: #0a111c; border: 1px solid var(--line); overflow: hidden; }\n.topic-bar i { display: block; height: 100%; background: linear-gradient(90deg, var(--blue), var(--gold)); }\n.org-row { display: grid; grid-template-columns: 52px minmax(0, 1fr) auto; gap: 10px; align-items: center; }\n.org-logo { display: grid; place-items: center; width: 46px; height: 38px; border: 1px solid var(--gold); background: #0b111b; font-weight: 900; color: var(--gold); }\n.overlap-wrap { overflow-x: auto; }\n.matrix-table { width: 100%; border-collapse: collapse; min-width: 560px; }\n.matrix-table th, .matrix-table td { border: 1px solid var(--line); padding: 0; text-align: center; }\n.matrix-table th { color: var(--muted); padding: 8px; background: #0c1420; }\n.matrix-table button { width: 100%; min-height: 42px; border: 0; color: var(--text); background: linear-gradient(90deg, rgba(142,199,255,.16), rgba(255,194,71, calc(var(--heat, 0%) / 120))); font-weight: 900; }\n.penetration-row { display: grid; grid-template-columns: 140px minmax(0, 1fr) 32px; gap: 8px; align-items: center; }\n.penetration-row span { display: flex; flex-wrap: wrap; gap: 4px; }\n.penetration-row i { border: 1px solid var(--line); color: var(--muted); padding: 3px 5px; font-style: normal; font-size: var(--fs-s); }\n.penetration-row i.hit { border-color: var(--gold); color: var(--gold); background: rgba(255,194,71,.09); }\n.migration-row { display: grid; gap: 7px; }\n.migration-row span { display: flex; flex-wrap: wrap; gap: 5px; }\n.migration-row i { border: 1px solid var(--line); background: rgba(142,199,255,.08); padding: 4px 6px; color: var(--blue); font-style: normal; font-size: var(--fs-s); }\n.influence-timeline { display: grid; grid-template-columns: repeat(auto-fit, minmax(92px, 1fr)); gap: 6px; }\n.month-cell { display: grid; gap: 4px; min-height: 72px; }\n.month-cell strong { color: var(--gold); }\n.month-cell span { color: var(--muted); overflow: hidden; display: -webkit-box; -webkit-line-clamp: 2; -webkit-box-orient: vertical; }\n.geo-row { display: grid; grid-template-columns: 30px minmax(0, 1fr) auto; gap: 8px; align-items: center; }\n.geo-row strong, .org-row strong, .penetration-row strong { white-space: nowrap; overflow: hidden; text-overflow: ellipsis; }\n@media (max-width: 980px) { .intel-grid { grid-template-columns: 1fr; } .topic-row, .org-row, .penetration-row { grid-template-columns: 1fr; } .matrix-table { min-width: 520px; } }\n`;
}
fs.writeFileSync(cssPath, css);
const pkg = JSON.parse(fs.readFileSync(pkgPath, 'utf8'));
pkg.version = '4.0.0';
pkg.description = 'Static OSM-derived influence cockpit with roadmovies, alerts, themes, organization profiles, overlap matrices, power geography and deep-capture planning.';
pkg.scripts = pkg.scripts || {};
pkg.scripts['crawl:alerts'] = 'node scripts/alert-theme-graph-planner.mjs';
if (!pkg.scripts.check.includes('crawl:alerts')) pkg.scripts.check = pkg.scripts.check.replace('npm run crawl:heavy-hitters', 'npm run crawl:heavy-hitters && npm run crawl:alerts');
if (!pkg.scripts['check:js'].includes('alert-theme-graph-planner')) pkg.scripts['check:js'] += ' && node --check scripts/alert-theme-graph-planner.mjs';
fs.writeFileSync(pkgPath, JSON.stringify(pkg, null, 2));
const planner = `import fs from "node:fs";\nimport path from "node:path";\nconst root = process.cwd();\nconst data = JSON.parse(fs.readFileSync(path.join(root, "data", "demo.json"), "utf8"));\nconst families = ["Davos","Bilderberg","Munich","FII","Jackson Hole","IMF / World Bank","G20","G7","NATO","COP","OPEC","BIS","ECB Sintra","Sun Valley","Milken","Raisina","Shangri-La","APEC","BRICS","UNGA"];\nconst topics = (data.topicTags || []).map(t => t.topic);\nconst orgs = (data.organizationProfiles || []).map(o => o.name);\nconst jobs = [];\nfor (const fam of families) {\n  jobs.push({ type: "agenda-change-watch", eventFamily: fam, sourceTargets: ["official agenda", "speaker list", "participant list", "PDF programme", "livestream metadata"], alertTypes: ["new attendee announced", "agenda published", "speaker added", "event moved", "registration opens"] });\n  for (const topic of topics) jobs.push({ type: "topic-extraction", eventFamily: fam, topic, fields: ["session title", "speaker", "organization", "year", "source URL"] });\n}\nfor (const org of orgs) jobs.push({ type: "organization-penetration", organization: org, eventFamilies: families, output: ["representative", "role", "event", "year", "source URL"] });\nconst out = { generatedAt: new Date().toISOString(), alerts: data.alerts?.length || 0, topicTags: topics.length, organizations: orgs.length, eventFamilies: families.length, plannedJobs: jobs.length, jobs };\nfs.mkdirSync(path.join(root, "data", "crawler"), { recursive: true });\nfs.writeFileSync(path.join(root, "data", "crawler", "alert-theme-graph-plan-v4.0.0.json"), JSON.stringify(out, null, 2));\nconsole.log(\`Alert/theme graph planner: \${jobs.length} jobs for \${families.length} event families, \${topics.length} topics and \${orgs.length} organizations.\`);\n`;
fs.writeFileSync(path.join(root, 'scripts', 'alert-theme-graph-planner.mjs'), planner);
const doc = `# ParleyMap v4.0.0 — Alerts, Themes and Event-Graph Intelligence\n\nThis iteration keeps the accepted v3.9 layout and adds the sticky intelligence layer below the map.\n\n## Added product modules\n\n- Alert center: new attendee announced, agenda published, speaker added, event moved / updated, registration opens.\n- Theme extraction: AI, defence, inflation, energy, China, semiconductors, climate, debt and trade.\n- Recurring attendees: most connected, rising attendees, new entrants and cross-network profiles.\n- Organization profiles: BlackRock, NATO, IMF, BIS, Microsoft, OpenAI, Brookings and CFR seed cards.\n- Event overlap matrix: shows attendee overlap between high-signal event families.\n- Organization penetration index: ranks institutions by recurring presence across event families.\n- Topic migration: shows how ideas move between venues over time.\n- Influence timeline: month-by-month power calendar.\n- Power geography: city-level clusters with people, event types and map focusing.\n\n## Crawler addition\n\nThe new script is \`scripts/alert-theme-graph-planner.mjs\`. It writes \`data/crawler/alert-theme-graph-plan-v4.0.0.json\` and plans jobs for agenda changes, speaker list changes, topic extraction and organization penetration scoring.\n\n## Safety boundary\n\nNo change to the publication boundary: no live tracking, private homes, hotel guesses, leaked itineraries, aircraft tracking, medical locations or private family locations. Alerts are public-source change alerts.\n`;
fs.writeFileSync(path.join(root,'docs','ITERATION_NOTES_v4.0.0.md'), doc);
fs.writeFileSync(path.join(root,'docs','ALERT_THEME_ORG_GRAPH_v4.0.0.md'), doc);
const readmePath = path.join(root,'README.md');
let readme = fs.existsSync(readmePath) ? fs.readFileSync(readmePath,'utf8') : '# ParleyMap\n';
readme = readme.replace(/v3\.9\.0/g, 'v4.0.0');
if (!readme.includes('v4.0.0 intelligence layer')) readme += `\n\n## v4.0.0 intelligence layer\n\nAdds alert center, topic radar, recurring-attendee panels, organization profiles, event overlap matrix, organization penetration index, topic migration, influence timeline and power geography.\n`;
fs.writeFileSync(readmePath, readme);
console.log('Patched template, CSS, package and docs for v4.0.0.');
