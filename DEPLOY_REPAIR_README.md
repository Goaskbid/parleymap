# ParleyMap homepage repair v3.9.1

The live repository currently has the corrections page saved as `index.html`. GitHub Pages therefore serves the corrections page at `https://parleymap.com/`.

Upload the contents of this folder directly into the repository root and replace existing files.

Required immediate replacement:

- `index.html`

Recommended together with it:

- `corrections.html`
- `impressum.html`
- `privacy.html`
- `data/operator.json`
- `CNAME`
- `.nojekyll`

Do not upload this README if you do not want it in the public repo. Do not upload the zip itself. Do not put these files inside a subfolder.
